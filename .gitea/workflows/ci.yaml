name: ci
on:
  push:
    branches: [main]
  pull_request:
jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Fetch the exact provider kit from the public source of truth
        run: |
          git init .ci/vistoda-provider-kit
          git -C .ci/vistoda-provider-kit remote add origin https://git.luigibarretta.com/luigibarretta/lib-vistoda-provider-kit.git
          git -C .ci/vistoda-provider-kit fetch --depth=1 origin 4f6c74125a673cb9836471122ae0007938b86619
          git -C .ci/vistoda-provider-kit checkout --detach FETCH_HEAD
      - name: Verify pinned Vistoda provider kit
        run: |
          pin="$(tr -d '[:space:]' < dependencies/vistoda-provider-kit.sha)"
          test "${pin}" = "4f6c74125a673cb9836471122ae0007938b86619"
          test "$(git -C .ci/vistoda-provider-kit rev-parse HEAD)" = "${pin}"
          cmp .ci/vistoda-provider-kit/dist/vistoda-app-bootstrap.sh \
            packaging/home-assistant/vistoda-app-bootstrap.sh
      - name: Install CI system dependencies
        run: |
          apt-get update
          apt-get install -y --no-install-recommends protobuf-compiler
          rm -rf /var/lib/apt/lists/*
      - uses: dtolnay/rust-toolchain@858a1ecb106d4516018578006590eb829de1f08e # 1.96.1
        with:
          components: clippy,rustfmt
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
      - run: cargo fmt --all -- --check
      - run: cargo clippy --locked --all-targets --all-features -- -D warnings
      - run: cargo test --locked --test loc_budget
      - run: cargo test --locked --all-targets
      - run: cargo install cargo-audit --locked
      - run: cargo audit --deny warnings
      - name: Build standalone image at the declared version
        shell: bash
        run: |
          version="$(sed -n 's/^version = "\(.*\)"/\1/p' Cargo.toml | head -1)"
          test -n "${version}"
          docker build --build-arg "VERSION=${version}+ci" --build-arg REVISION=ci \
            --tag ring-intercom-bridge:test .
          test "$(docker image inspect ring-intercom-bridge:test \
            --format '{{ index .Config.Labels "org.opencontainers.image.version" }}')" \
            = "${version}+ci"
      - run: >-
          docker run --rm --network none --read-only --user 10001:10001
          ring-intercom-bridge:test --version
